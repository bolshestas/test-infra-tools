apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasource
  namespace: my-grafana
data:
  datasource.yaml: |
    apiVersion: 1

    datasources:
      - name: Prometheus
        uid: dfvbc91goms5cb
        type: prometheus
        access: proxy
        url: http://prometheus-service.prometheus.svc.cluster.local
        isDefault: true
        editable: false
